package sortT;

import java.util.Arrays;
import java.util.stream.IntStream;
import java.util.stream.Stream;

public class MainSor {

	public static void main(String[] args) {

//		Stream<String> stream = Stream.of("Hallo", "hallo,", "hallo", "Bob");
//
//		int result = stream.flatMap(s -> Arrays.stream(s.split(" "))).map(word ->1).reduce(0,(sum,word)-> ++sum);
		// System.out.println(result);

		// stream.flatMap(s -> Arrays.stream(s.split(""))).map(s ->
		// s.toUpperCase()).forEach(System.out::print);
		// stream.filter(s -> s.startsWith("h")).forEach(System.out::print);
		
		

	}

}
